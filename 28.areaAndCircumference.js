const area = r => {
  let area = 3.14 * r * r;
  let circumferece = 2 * 3.14 * r;
  console.log(`Area of a circle is ${area}`);
  console.log(`Circumferece of a circle is ${circumferece}`);
};

area(10);
